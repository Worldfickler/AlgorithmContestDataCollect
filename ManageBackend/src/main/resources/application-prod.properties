logging.config=classpath:log4j2.xml
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.url=${DB_URL}
spring.redis.host=${REDIS_HOST}
spring.redis.port=${REDIS_PORT:6379}
spring.redis.password=${REDIS_PASSWORD}
security.rootUsername=${ROOT_USER_NAME}
security.rootPassword=${ROOT_USER_PASSWORD}
jwt.secret=asdfsdafsdfgsdcasdcewg
jwt.expire=3600000
jwt.issuer=ACDC/MANAGE
allow.WxBot=true
server.port=${MANAGER_BACKEND_PORT:8085}
WxBotUrl=${WX_BOT_URL}
crawlerTask.stream=${CRAWLERTASK_STREAM:tCrawlerTasks}